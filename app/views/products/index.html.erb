<h1> Here's a list of <%= @user.name %>'s  products </h1>
<ul>
	<% @products.each do |product| %>
		<li><%= link_to product.title, user_product_path(:user_id => @user.id, :id => product.id) %></li>
	<% end %>
</ul>

	
